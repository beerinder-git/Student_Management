@page
@model Student_Management.Pages.StudentEnrollment.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student_Enrollment[0].student_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student_Enrollment[0].tutor_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student_Enrollment[0].course_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Student_Enrollment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.student_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tutor_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.course_ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.student_Enrollment_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.student_Enrollment_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.student_Enrollment_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
